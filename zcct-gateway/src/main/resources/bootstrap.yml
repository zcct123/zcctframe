# Tomcat
server:
  port: 80

# Spring
spring:
  application:
    name: zcct-gateway
  profiles:
    active: dev
  cloud:
    nacos:
      username: nacos
      password: nacos
      discovery:
        server-addr: 127.0.0.1:8848
        namespace: zcctframe
        group: gateway-${spring.profiles.active}
      config:
        server-addr: 127.0.0.1:8848
        file-extension: yml
        namespace: zcctframe
        group: gateway-${spring.profiles.active}
        shared-configs:
          - application-${spring.profiles.active}.${spring.cloud.nacos.config.file-extension}
